### How internet works?

- The internet is a network of networks
- Transfer the data
- data is split into small packet
- Routes direct these packet to different path
- At the destination data reassembled
- Packet Switching: packet switching means data is transferred from one device to another device through packets, so packets are 
disassembled from source and assembled again at destination.

## IP/TCP, HTTP, UDP, HTTPS
